rules_version = '2';

// Sinfagram Storage security rules.
// Baseline: only signed-in users can read or upload.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
